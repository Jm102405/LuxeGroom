{
  // Database connection string — SQL Server Express with Windows Authentication
  "ConnectionStrings": {
    // LocalDB instance on LAPTOP-9AUO1OS1, targeting LuxeGroomDB catalog
    "DefaultConnection": "Data Source=RICSON\\SQLEXPRESS;Initial Catalog=LuxeGroomDB;Integrated Security=True;TrustServerCertificate=True"
  },

  // Gmail SMTP settings used by EmailService.cs for OTP and welcome emails
  "EmailSettings": {
    // Sender Gmail address used as the From address
    "SenderEmail": "ccs.jaysonmiguel@gmail.com",
    // Gmail App Password — generated from Google Account > Security > App Passwords
    "SenderPassword": "nger pwea ragy wkax",
    // Gmail SMTP host
    "SmtpHost": "smtp.gmail.com",
    // Gmail SMTP port — 587 uses STARTTLS with SSL enabled
    "SmtpPort": "587"
  },

  // ASP.NET Core logging configuration
  "Logging": {
    "LogLevel": {
      // Default log level for all categories
      "Default": "Information",
      // Suppress verbose framework logs — only warnings and above
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // Allow requests from any host — restrict in production
  "AllowedHosts": "*"
}
